#ifndef __credentials_H__
#define __credentials_H__

extern const char *WiFi_SSID, *WiFi_PW, *AP_SSID, *AP_PW, *mDNSname;

extern const char *mqtt_server, *mqtt_username, *mqtt_password;
extern const char *inTopic, *outTopic, *outTopic_debug;

#endif